html
    head
        title #{document.title}
        link(href="http://fonts.googleapis.com/css?family=Cinzel+Decorative:400,700|Open+Sans:300,400,700", rel="stylesheet")
        != getBlock("styles").add(["/styles/style.css"]).toHTML()

    body(class="#{document.class}")
        header
            test
        section
            | !{content}
        footer
            test
        script(src="/vendor/jquery-2.0.3.min.js")
        script(src="/scripts/app.js")
